# 2026-08-11

## 自适应文本面试演进文档（算法面试推迟）

- 决策：算法面试（代码沙箱）推迟，自适应文本面试优先级提前为唯一演进主线。
- 关键事实发现（读代码确认）：实际 MVP 是 `agent-loop-mvp-v1`（`modules/interview/agent/`），与原 MVP 设计文档不一致——走的是 Skill 人格（load_skill + SKILL.md persona + sha256 冻结）+ 有界自由循环（≤3 模型步/轮、≤1 工具调用、30s deadline、单问题校验、@Version 乐观锁）路线；出题侧已自适应，但评估侧为空（Turn 只有 question/answer，无评级、无证据、无报告）。
- 产出 `AGENT_ADAPTIVE_TEXT_INTERVIEW_EVOLUTION.md`：E1 每轮深度评估+原文证据（两次独立 LLM 调用，评估可见内容但不可见历史评级；评估不占 runBounded 的 3 步预算）→ E2 考察计划（planJson 字段，skill 从整场一人格演进为每维度一人格）→ E3 动态轮次预算+维度状态机（maxTurns 从 6 改为 min(维度数×2, 12)）→ E4 确定性证据报告 API+前端 → E5 上下文压缩（维度小结只导航不进报告）。
- 同步修订：平台设计文档路线图 Phase 0 改为以代码为准、Phase 2 标记已推迟并附演进文档链接；旧 MVP 设计文档标记为历史文档。
- 北极星指标：证据可追溯率 100% + 评级一致性（相同回答在不同上下文污染下评级一致）。
- 用户明确表示满意平台设计的多 Agent 方案；演进文档新增 3.1 节显式映射（编排器=InterviewAgentLoop、评估=E1 assess、规划=E2、维度面试官=skill persona），并记录三处有意偏离：不上报告措辞 Agent（模板先行）、维度面试官不建独立类（persona 即独立 Prompt）、评估不物理分离成服务（分离在调用/上下文层面）。
