# 2026-08-12

## 实现文档重写：抛弃 v1 代码约束，以平台设计为蓝本（评估后置）

- 用户决策：抛弃 agent-loop-mvp-v1 代码约束；"第一版"（平台设计文档）为准；重写实现文档，注重 ReAct 范式、长期/短期 memory、多 Agent 角色拆分、工具 function calling、MCP；评估之后再做。
- "React 范式"按 ReAct（Reasoning+Acting）理解并在文档中声明，非前端框架。
- 重写 `AGENT_ADAPTIVE_TEXT_INTERVIEW_EVOLUTION.md` 为 M0~M5 实现设计：
  - M0 ReAct 内核（thought+action 结构化输出、有界三件套、裁决在编排器、thought 不落全文只落决策摘要）→ M1 规划 Agent+维度状态机 → M2 工具 function calling（白名单按角色）→ M3 记忆（短期压缩+长期画像，写入纪律从 M3 就强制）→ M4 MCP（客户端接外部题库 dogfooding、服务端带租户模型）→ M5 评估体系（量规/证据/报告/回填）。
  - 核心不变量：评估可后置，证据不可后置——agent_turns 从 M0 起结构化完整落库（不用聚合 JSON），保证 M5 回填；CI schema 守护测试。
  - 角色=配置不是类层级（Prompt+上下文规则+工具白名单+预算，AgentRoleRegistry）。
  - v1 处置：开关关闭、只读归档、新代码物理隔离；只吸收 4 件经验（deadline 虚拟线程、单问题校验、skill 哈希冻结、乐观锁），不 import 旧类。
- 同步修订平台设计文档 Phase 1 行与路线图注记。注意：用户手改过 Phase 1 行（"早期继续使用带版本聚合 JSON"），与新文档"agent_turns M0 起结构化"存在分歧，已在回复中请用户裁决（折中：会话状态可 JSON，轮次原文必须结构化行）。
- 用户曾在 2026-08-11 明确满意多 Agent 角色拆分方案；本次保留该方案全部价值、只简化实现形式。

## 裁决落地：turn 结构化成行，不兼容旧设计

- 用户裁决（2026-08-12 晚）：按助手方案执行，不需要兼容以前的设计，turn 直接结构化成行方便观测。
- 已落实：演进文档 1.1 标记"已裁决确认"（不兼容 v1 存储、不双写；agent_turns M0 起成行；仅会话/循环瞬时状态可用 JSON）；平台设计文档第 8 节迁移策略改为"不做兼容、不做双写，v1 只读归档"，作废旧"双写迁移"说法。
- 长期项目约定：凡是要被查询、聚合、回填的数据一律成行；JSON 字段只允许承载瞬时状态。
## 算法题面试设计：沙箱工具 + 延迟执行（新文档）

- 用户提出补充算法题面试：给 Agent 代码沙箱工具，执行可延后。产出 `AGENT_ALGORITHM_INTERVIEW_DESIGN.md`（根目录，待评审）。
- 核心设计：延迟执行是一等公民语义——`sandbox_submit` 立即返回 PENDING 句柄，结果走 Redis Stream（复用 AbstractStreamProducer/Consumer）异步落库后以 ToolResultReady 事件唤醒面试官新循环；**刻意不提供轮询工具**（防模型轮询烧 token）。
- 内核唯一框架级改动：工具结果允许为 Pending，循环正常终止 + 事件触发新循环（复用"候选人提交回答"唤醒链路）；事件等待不计入步预算/deadline。
- 三裁决点在代码：执行配额（默认 20 次/场 + @RateLimit）、时效裁决（v1 结果迟到且已有 v2 → 落库标 superseded 不唤醒）、降级裁决（排队 90s 超时 → 面试官转代码走读，结果后补）。
- verdict 分 AC/WA/CE/TLE/MLE/RE（候选人客观事实）vs IE（平台事故，恒不计负面证据，可重判回填）；隐藏用例内容永不进 LLM 上下文。
- 新表：algorithm_problems / sandbox_executions（证据不可后置载体）/ sandbox_execution_logs（大字段存 S3）。
- 阶段 A0~A3：A0 依赖 M0+M2，A3 依赖 M5 评估回填；与代码分析专项设计（MCP Pi SDK 只读分析）边界不合并。
- 待办：评审通过后需更新平台设计 §11 "Phase 2 已推迟"注记。

## 文档体系整理：docs/ 层级化 + 总入口（配合重实现决策）

- 用户决策：按文档从零重新实现面试 Agent，不考虑 v1 现役代码。要求整理文档层级与入口。
- 新结构：`docs/design/`（重实现蓝图，唯一事实源）+ `docs/archive/`（历史）+ `docs/README.md`（总入口，含阅读顺序、状态表、依赖图、三条全局不变量、维护规则）。
- 重命名映射：CONTEXT.md→design/00-terminology.md；AGENT_INTERVIEW_PLATFORM_DESIGN.md→design/01-platform-design.md；AGENT_ADAPTIVE_TEXT_INTERVIEW_EVOLUTION.md→design/10-text-interview.md；AGENT_ALGORITHM_INTERVIEW_DESIGN.md→design/11-algorithm-interview.md；AGENT_PROJECT_CODE_ANALYSIS_DESIGN.md→design/12-code-analysis-service.md；agent-evolution.html→design/agent-evolution-map.html；AGENT_INTERVIEW_MVP_DESIGN.md→archive/mvp-design.md；BASELINE.md→archive/baseline-2026-07.md。
- 注意：这批设计文档大多未被 git 跟踪，git mv 会报 "not under version control"，改用普通 mv。
- 已同步：全部交叉引用、根 README.md 加重实现指引段落、AGENTS.md Project Structure 加 docs/ 条目；voice-interview-architecture.md 留在 docs/ 根（现役功能文档）。
- 维护规则约定：新专项设计从 10 起递增编号并登记到 docs/README.md；被取代文档移 archive/ 不删除。
